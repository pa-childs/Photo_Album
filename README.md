# Photo Album

Photo album web application (Python Flask app)

Allows the user to place sets of images within that web application. All image sets can be viewed on the initial page, and when a set is loaded all if the contained images are displayed for that set.

* Each set has a meta.json file when contains:
  * Title
  * Tags
  * Description
* Image Sets on the main page can be filtered by Tags
